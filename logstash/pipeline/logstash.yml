# Paramètres globaux de Logstash
node.name: "my-logstash-node"
path.data: ../../data/books.csv
http.host: "0.0.0.0"
http.port: 9200

# Configurations de monitoring (X-Pack)
xpack.monitoring.elasticsearch.hosts: ["http://elasticsearch:9200"]
